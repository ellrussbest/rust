// if you have lib.rs defined in your source directory,
// then a library crate with the same name as the package
// will be automatically created and lib.rs will be the crate root

// the crate root is the source file that rust compiler starts at when
// building the crate and it also makes up the root module of the crate!
